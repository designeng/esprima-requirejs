define([
    "globalEvents",
    "tableView"
], function (globalEvents, tableView){
    var start = function (opt){
        console.log(opt)
    }
    var core = {
        start: start
    }
    return core;
});